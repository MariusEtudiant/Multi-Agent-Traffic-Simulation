/*
package org.example.gui;

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.layout.Pane;
import javafx.stage.Stage;

public class Main extends Application {
    @Override
    public void start(Stage primaryStage) {
        // Création d'un Pane qui contiendra les éléments graphiques
        Pane root = new Pane();

        // Création de la SimulationView avec le Pane comme conteneur
        SimulationView simulationView = new SimulationView(root);

        simulationView.setSimulationSpeed(0.1);
        // Création de la scène avec le Pane racine
        Scene scene = new Scene(root, 1200, 800);

        primaryStage.setTitle("Simulation de trafic");
        primaryStage.setScene(scene);
        primaryStage.show();

        // L'animation démarre automatiquement via le start() dans le constructeur de SimulationView
    }

    public static void main(String[] args) {
        launch(args);
    }
}

 */